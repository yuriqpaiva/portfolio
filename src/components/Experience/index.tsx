import { Flex } from '@chakra-ui/react';
import { SectionTitle } from '../utils/SectionTitle';

export function Experience() {
  return (
    <Flex width="100%" mt="40">
      <SectionTitle name="Experience" number={2} justify="center" />
    </Flex>
  );
}
